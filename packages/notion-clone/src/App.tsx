import React from "react";
import { HelloWorld } from "my-module";

const App = (): React.ReactElement => {
  return (
    <div>
      App을 시작해볼까?
      <HelloWorld />
    </div>
  );
};

export default App;
